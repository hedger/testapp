on: [push]

jobs:
  test_ubt_build_action:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: 'Build for dev channel'
            sdk-channel: 'dev'
          - name: 'Build for release'
            sdk-channel: 'rc'
          - name: 'Build for Unleashed'
            sdk-channel: 'dev'
            sdk-index-url: https://up.unleashedflip.com/directory.json

    name: "ufbt: ${{ matrix.name }}"
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build with ufbt
        uses: hedger/flipperzero-ufbt-action@dev
        id: build-app
        with:
          ufbt-version: 'prerelease'
          sdk-channel: ${{ matrix.sdk-channel }}
          isdk-ndex-url: ${{ matrix.sdk-index-url }}

      - name: Upload app artifacts
        uses: actions/upload-artifact@v3
        with:
          name: fap-${{ steps.build-app.outputs.suffix }}
          path: ${{ steps.build-app.outputs.fap-artifacts }}

